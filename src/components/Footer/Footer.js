import React from "react";
import classNames from "classnames";

export function Footer({ className }) {
    return (
        <footer className={classNames('footer', className)}>epashata@gmail.com</footer>
    )
}
